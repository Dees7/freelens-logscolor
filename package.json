{
  "name": "freelens-logscolor",
  "version": "1.0.0",
  "description": "Раскраска логов в штатном просмотрщике Freelens 1.x и Lens 6.x: JSON, logfmt, klog, уровни, стектрейсы",
  "publisher": "@freelens-logscolor/freelens-logscolor",
  "license": "BSD-3-Clause",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Dees7/freelens-logscolor.git"
  },
  "renderer": "dist/renderer.js",
  "files": [
    "dist/renderer.js",
    "README.md",
    "logscolor.png"
  ],
  "engines": {
    "node": ">=16",
    "freelens": "^1.0.0",
    "lens": "^6.0.0"
  },
  "keywords": [
    "freelens",
    "lens",
    "k8slens",
    "extension",
    "kubernetes",
    "logs",
    "colors",
    "ansi"
  ],
  "scripts": {
    "build": "npm run clean && vite build",
    "clean": "rm -rf ./dist",
    "start": "vite build --watch",
    "test": "vitest run",
    "type:check": "tsc -p tsconfig.json",
    "smoke": "npm run build && node scripts/smoke.mjs",
    "check": "npm run type:check && npm test && npm run smoke",
    "pack": "npm run check && npm pack"
  },
  "devDependencies": {
    "@freelensapp/extensions": "^1.10.3",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
